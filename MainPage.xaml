<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Proyecto_Grupo3.MainPage"
             Title="Gestión de Productos"
             Background="Transparent">
    <Grid>
        <Image Source="fondo.jpg"
               Aspect="AspectFill"
               Opacity="0.6"
               HorizontalOptions="FillAndExpand"
               VerticalOptions="FillAndExpand" />

        <Grid RowDefinitions="Auto, *, Auto"
              ColumnDefinitions="*"
              Padding="20"
              HorizontalOptions="FillAndExpand"
              VerticalOptions="FillAndExpand">

            <Label Text="Sistema de Gestión de Inventario"
                   Grid.Row="0"
                   FontSize="32" TextColor="White"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"
                   Margin="0,200,0,20" FontFamily="Comic Sans MS"/>

            <ListView Grid.Row="1"
                      Grid.Column="0"
                      x:Name="ItemsListView"
                      SelectionMode="Single"
                      Margin="0,10,0,10"
                      Background="Transparent">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid Padding="10" BackgroundColor="#E0E0E0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Label Grid.Row="0" Grid.Column="0" Text="{Binding Nombre}" FontSize="18" TextColor="Black" FontAttributes="Bold" />
                                <Label Grid.Row="0" Grid.Column="1" Text="{Binding ID, StringFormat='ID: {0}'}" FontSize="14" TextColor="Gray" HorizontalTextAlignment="End"/>
                                <Label Grid.Row="1" Grid.Column="0" Text="{Binding Descripcion}" FontSize="14" TextColor="DarkGray" />
                                <Label Grid.Row="1" Grid.Column="1" Text="{Binding Cantidad, StringFormat='Cant: {0}'}" FontSize="14" TextColor="DarkCyan" HorizontalTextAlignment="End"/>
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <VerticalStackLayout Grid.Row="2"
                                 Grid.Column="0"
                                 HorizontalOptions="Center"
                                 VerticalOptions="Center"
                                 Spacing="10">
                <Button Text="Agregar Producto"
                        Background="#0066CC"
                        TextColor="White"
                        FontSize="16"
                        CornerRadius="8"
                        Padding="15,10"
                        WidthRequest="200"
                        Clicked="OnAgregarProductoClicked"  HorizontalOptions="Center"/>

                <Button Text="Ir a la segunda página"
                        Background="#0066CC"
                        TextColor="White"
                        FontSize="16"
                        CornerRadius="8"
                        Padding="15,10"
                        WidthRequest="200"
                        Clicked="OnNavigateButtonClicked"
                        HorizontalOptions="Center"/>
            </VerticalStackLayout>
        </Grid>
    </Grid>
</ContentPage>